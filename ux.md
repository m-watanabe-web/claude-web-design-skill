# UX要件（forbid.md より先に満たすこと）

**使えないものを綺麗にしても意味がない。** Step 0 はこのファイル、見た目の禁止は その後。

根拠：WebAIM Million 2026 は上位100万ページの95%超に欠陥を検出し、全エラーの96%が
7年連続で同じ6カテゴリに集中している。AI生成コードはこれをほぼ考慮しない
（ACM SIGACCESS 第27回：AI生成6サイトから308件のエラー、うちWCAG違反47.1%）。

## A. 必須（機械検証できる。`measure.py` が判定）

WebAIMの頻出6カテゴリに対応する。

1. **コントラスト** — 本文 4.5:1 以上、大文字(18.66px以上/24px)とUI境界 3:1 以上（WCAG 2.2 AA）
   └ 薄いグレー文字は最頻出の違反。`opacity` / `color-mix` の多用で簡単に落ちる。
2. **`alt` 属性** — 装飾画像は `alt=""`。中身のある画像は内容を書く。
   └ 「image of a person」のような無内容な記述は書かない。捏造しない。
3. **フォームのラベル** — 全入力に `<label for>` を紐づける。placeholder はラベルの代用にならない。
4. **空リンク・空ボタンを作らない** — `href="#"` のダミーを残さない。アイコンのみのボタンには `aria-label`。
5. **`<html lang="ja">`** を必ず付ける。
6. **見出し階層を飛ばさない** — h1 は1つ。h1→h3 の飛びを作らない。

## B. 必須（構造・操作）

7. **`:focus-visible` を必ず定義する** — キーボード操作の可視化。`outline:none` の打ちっぱなし禁止。
8. **`prefers-reduced-motion` に対応する** — アニメーションを入れるなら必ずセットで無効化経路を用意。
   └ 前庭障害のあるユーザーに実害が出る。「動きを入れない」だけでは対応にならない。
9. **ランドマークを置く** — `<main>` は必須。`<header>` `<nav>` `<footer>` を適切に。
10. **タップ領域 44×44px 以上**（WCAG 2.2 の Target Size）。
11. **`<meta name="viewport">`** と、実機幅で破綻しないレイアウト。
    └ `contrast.py` が 1280px / 380px の2幅で横溢れを検査する。
      grid の `1fr` は `minmax(0, 1fr)` にしないと、中の固定幅要素（iframe・表など）が
      min-content を押し上げて列が画面外に出る。**実際にこれで比較ページが壊れた。**

## C. 推奨

12. **`prefers-color-scheme` 対応**（両テーマ）。
13. 数値を並べる面では `tabular-nums`。
14. フォームのエラーは「何が・なぜ・どう直すか」を文章で示す。色だけで伝えない。

## 検証で分かったこと（2026-08-19・重要）

**禁止リスト（見た目）だけを適用すると、UXは対照群より悪化した。**

| 指標 | 対照群（リストなし） | 見た目リスト適用 |
| --- | --- | --- |
| `:focus` | 2 | **0** |
| `prefers-reduced-motion` | あり | **なし** |
| `<main>` | あり | **なし** |
| メディアクエリ | 9 | **1** |
| 空リンク | 0 | **1** |
| コントラスト | — | **2.70:1（不合格）** |

「角丸を使うな」「アニメを入れるな」「短くしろ」と縛った結果、
フォーカス表示と reduced-motion が削られた。**対照群は指示なしでそれらを実装していた。**
→ 見た目の制約は、放っておくとUXを削る方向に働く。このファイルが先にある理由。

## 位置づけ（NN/g "State of UX 2026"）

> UIは依然として重要だが、**差別化要因ではなくなっていく**。AIの標準化で誰でも
> それなりの見た目を作れるようになるため。

見た目のAI感を消す作業に、長期の優位性は期待しない。**差がつくのはこのファイルの側。**

## 参照（実証ベース）

| 情報源 | 性質 | URL |
| --- | --- | --- |
| WCAG 2.2 | 規格。機械判定可 | https://www.w3.org/TR/WCAG22/ |
| Baymard Institute | 42,000時間の実測・700+ガイドライン | https://baymard.com/ |
| NN/g | ヒューリスティクスとユーザビリティテスト | https://www.nngroup.com/articles/ |
| GOV.UK Design System | 公共サービスでの実運用検証。デジタル庁DSの源流 | https://design-system.service.gov.uk/ |
| WebAIM Million | 100万ページの年次実測 | https://webaim.org/projects/million/ |
