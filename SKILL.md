---
name: web-design
description: Webサイト・LP・UIを「AI感なく」作るための実装スキル。「LPを作って」「サイトのデザインを起こして」「ヒーローを組んで」「このページの見た目を良くして」「AIっぽさを消して」「デザインの方向性を複数出して」等、ブラウザで表示するページ／画面の外観を作る依頼で使う。禁止リスト（検証済み）・design-spec による仕様固定・発散収束の手順が核。既存文章の図解化は visual-report、グラフは dataviz、Artifact成果物の体裁は artifact-design に委譲する。
---

# Webデザイン実装ワークフロー

**「センスを足す」のではなく「選択肢を奪い、外部の型を渡す」**ことでAI感を消す。

## いつ使う / 使わない
- **使う**：LP・Webサイト・アプリ画面・ヒーロー等、**ブラウザで表示する画面の外観**を作る／直す。
- **使う**：既存プロダクトのUIレビュー（ナビ・ボタン・ラベル）。→ `interaction.md`
- **使わない**：既存の文章・レポートの図解化（→ `visual-report`）。数値グラフ単体（→ `dataviz`）。
  Artifactとして公開するHTML成果物の体裁（→ `artifact-design`。ただし本スキルの `forbid.md` は併用してよい）。

## 手順

### Step 0. UX要件 → 禁止リストの順で読む（必須）
1. **`ux.md` を先に読む。** 使えないものを綺麗にしても意味がない。
2. **次に `forbid.md`。** 各項目に検証ステータスが付いている。
3. **画面に操作（ナビ・ボタン・フォーム）があるなら `interaction.md` も読む。**
   メニュー配置・ボタンのラベル・フォームの層。**このファイルだけは対照実験を通していない**ので、
   根拠の強さが違うことを踏まえて使う（各項目に出所を明記してある）。

**この順序には実測の根拠がある。** 見た目の禁止リストだけを適用すると、
フォーカス表示・`prefers-reduced-motion`・`<main>` が削られ、
**対照群（何も指示しない条件）よりUXが悪化した**。詳細は `references/verification-2026-08.md`。

### Step 1. 白黒のワイヤーフレームだけ作る
いきなり装飾に入らない。**構成だけを白黒で**確定させる。
レイアウト案は10パターン程度発散させ、2案に絞る。

> 『Refactoring UI』（Wathan & Schoger）が同じ順序を勧めている
> —— 白黒で設計してから色を足すと、色に頼る前に間隔・コントラスト・活字で階層を作らざるを得なくなる。

### Step 2. レイアウトを1つ確定 → 次に色だけ入れる
**レイアウト・色・装飾を同時に動かさない。** 1変数ずつ収束させる。
「一旦、色だけ入れて。デザインは入れ込まないで」と明示的に区切る。

間隔・活字・色は**制約されたスケール**に載せる（任意の値を使わない）。
余白は多めに取ってから削る。アイコンや影の作り込みは、階層が機能してから。

### Step 3. 方向性をデザイン用語で指定する
一言で大量の文脈が乗る。非デザイナーほど効く。
- 参照デザインシステムを**名指しする**：Apple HIG / IBM Carbon / Material Design 3 / デジタル庁DS / Adobe Spectrum
- テイストを言語化する：「紙とインクの質感」「ネオブルータリズム」「設計図・方眼」「ダークミニマル」

### Step 4. design-spec で仕様を固定する
`design-spec.template.yaml` をコピーして埋める。毎回プロンプトで言い直さないための資産化。

### Step 5. AI感を消す仕上げを1〜2個入れる
全部入れる必要はない。**1つ2つで十分**。
| 系統 | 具体策 |
| --- | --- |
| 素材 | **本物の写真・動画**（最も効くとされるが未検証。オフラインでは使えない） |
| 質感 | SVG の `feTurbulence` による紙目、方眼・罫線の地 |
| 動き | スクロール連動（GSAP）。画像を固定して片側だけ動かす |
| 表現 | タイポグラフィ主体、非対称レイアウト、帳簿組み（カードにしない） |
| 生成 | p5.js でリロードごとに変わる背景 |

### Step 6. ツールを役割で使い分ける
設計と機能実装でモデル／ツールを分ける。Gemini・v0・Google Stitch 等。
※ 特定ツールの優劣は未検証。ツールの世代交代が速い領域なので、`design-spec` 側に仕様を持たせ、
ツールを差し替えても出力の芯が変わらない状態にしておく。

## 完成の判定（提出前に必ず実行）

```
python3 references/measure.py  <file.html>     # UX + 見た目の静的チェック
python3 references/contrast.py <file.html>     # コントラスト実測（Chrome headless）
```
`measure.py` の UX 行に `X` が残っていたら未完成。`contrast.py` の違反は 0 件にする。
**静的解析はコントラストを取り逃す**（入れ子の背景を追えない）ので、両方を必ず走らせる。

## 検証で分かっていること（重要）

2026-08-19 に A/B 検証を実施（対照群＝リストなしで白紙エージェントが生成、各条件 n=1、Opus 5、題材2種）。

- **効いた**：`border-radius` の抑制、均一カードグリッドの排除、無意味アニメの排除、冗長さの抑制
- **空振り**：絵文字の禁止、紫グラデーションの禁止（**対照群でも0件**）
- **逆効果**：見た目リスト単独の適用は、**UXを対照群より悪化させた**（→ `ux.md` を先に置く理由）
- **両方適用すれば両立する**：UX全項目クリア＋見た目クリアを1本で達成済み（`samples/B2-c-portfolio.html`）
- **未検証**：Step 1・2 のプロセス論（一発生成では差が出ない）、実写真の効果、ツール比較

詳細と限界は `references/verification-2026-08.md`。

## リストの育て方（これが本体）

**主張の強調度と実効性は一致しない。** 一般に最も強調されている2項目（紫グラデ・絵文字）が、
測ると何も禁止していなかった。広く言われていることほど、検証されないまま流通している。

新しい禁止項目を足すときは必ず：
1. 対照群（その項目を渡さない条件）を**別セッション／サブエージェント**で生成する
2. 機械測定で比較する（主観で判定しない。私は生成者なのでバイアスがかかる）
3. 差が出た項目だけ `forbid.md` に入れ、**検証ステータスを併記**する
4. **測定スクリプト自体のバグを疑う。過去に4件混入した実績がある**
   （矢印を絵文字と誤判定／`)}` で終わるgradientの取り逃し／静的解析でのコントラスト誤検出/見逃し／
   Chrome が `color-mix()` を `color(srgb ...)` で返すため全要素を黙って検査対象外にしていた）。
   **測定器が「違反0件」と言ったら、まず測定器を疑う。**

## 参考文献

手法の下敷きは『Refactoring UI』（https://refactoringui.com/ ）。
現象の記録は Kyle Chayka「The generic style of AI web design」および NN/g「State of UX 2026」。
UXの根拠は WCAG 2.2 / WebAIM Million / ACM SIGACCESS。詳細は README.md。

## 守秘
本人・顧客の固有名を出さない。抽象化の規約は `visual-report` の守秘ガードに従う。
