# 禁止リスト（AI感の除去）

各項目に**検証ステータス**を付す。2026-08-19 の A/B 検証（対照群＝禁止リストなしで白紙エージェントが生成、
各条件 n=1、モデル Opus 5、題材2種）の結果に基づく。詳細は `references/verification-2026-08.md`。

## 効果を確認済み（必ず守る）

1. **`border-radius` を既定で使わない。** 使う場合は理由を述べる。
   └ 対照群は両題材とも6箇所で使用。最も安定して差が出た項目。
2. **同サイズのカードを3枚以上横並びにしない。**
   └ 対照群は 3件 / 2件。
3. **装飾目的の均一グリッド（`repeat(3, 1fr)` / `repeat(auto-fit, ...)`）を使わない。**
   └ 上と同根。グリッドの均一性そのものが「AI感」の実体。
   └ **例外**：比較表・仕様書のように**列そのものが情報**である場合は可。
     ただし浮いたカードではなく**罫線の欄組み**にすること（`index.html` の3条件比較が該当）。
   └ 旧版は「使わない」の一点張りで、機能上必要な比較レイアウトまで禁じていた。
     `forbid.md` 内の「あらゆるグラデーション禁止」と同じ失敗（対象を絞らずに書いた）。
4. **意味のないアニメーションを入れない。**
   └ 対照群は3件（フェードイン等の装飾目的）。
5. **出力を必要以上に長くしない。**
   └ 対照群は 7倍（44KB / 21KB に対し 6KB / 5KB）。冗長さ自体が兆候。
6. **「アイコン＋見出し＋説明文」のセットを全セクションで反復しない。**
   └ 未計測だが 2 と同じ構造的反復。

## 表現を修正した項目

7. **色相が変化するグラデーションを禁止。**
   単色→透明のフェード、罫線・方眼の描画に使う `linear-gradient` は**可**。
   └ 旧版「あらゆるグラデーション禁止」は罫線描画まで巻き込む欠陥があった。
8. **色相は3つまで**（同一色相の濃淡は制限しない）。
   └ 旧版「色は3色まで」は濃淡を数えてしまい基準として機能しなかった。

## 空振りだった項目（削除済み・復活させない）

- ~~絵文字を使わない~~ — **対照群でも0件**。現行モデルは指示なしでもUIに絵文字を入れない。
- ~~紫〜青系グラデーションを禁止~~ — **対照群でも0件**。対照群が選んだのは緑→金→橙、および紺＋青。
  「紫グラデ＝AI感」は過去のモデル世代の話で、現行では該当しない。

> この2項目は「AIっぽさ」の代表例として最も広く挙げられているが、検証すると何も禁止していなかった。
> **主張の強調度と実効性は一致しない。** 新項目を足すときも必ず対照群と比較すること。

## 未検証（守ってよいが根拠は薄い）

- Tailwind / shadcn のデフォルト外観をそのまま使わない
- 中央寄せカードの羅列でページを構成しない
