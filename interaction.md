# インタラクション設計（メニュー・ボタン・ラベル）

`ux.md` が「機械判定できるアクセシビリティ」を扱うのに対し、ここは**配置と言葉**を扱う。

> **検証ステータスについて。**
> このファイルの項目は `forbid.md` と違い、**対照実験を通していない**。
> 根拠は (a) 実在プロダクトのレビューで実際に問題として現れたもの、(b) 実証ベースの外部資料。
> 各項目に出所を書く。**「実測済み」と混同しないこと。**

## A. ナビゲーション

1. **並ぶ位置を固定する。** `flex-wrap` で折り返し位置が幅に依存する横並びナビを作らない。
   位置が動くと「左下のあれ」で覚えられなくなる。
   └ 出所：実例レビュー（サイドバー中段が幅により折り返し位置が変わっていた）
2. **現在地を示す。** 現在のパスと一致する項目に `aria-current="page"` と視覚的な選択状態を両方付ける。
   └ 出所：実例レビュー（`aria-current` がアプリ全体で0箇所。どの画面にいるか読めなかった）
3. **序列を付ける。** 毎日使う項目と、月1回の管理系を同じ視覚的重みで並べない。
   段を分けるか、重みを変える。
4. **セッションを終える操作（ログアウト）を通常の移動と同じ列に置かない。** 末尾に分離する。
5. **ラベルから行き先が推測できること。** 実装のルート名と表示名が乖離していないか確認する
   （例：「季節のモチーフ」→ `/calendar` は推測できない）。
6. **ラベルの粒度を揃える。** 「場所」「動作」「内容」を混ぜない。
7. **`<nav>` を使う。** 複数のナビがあるなら `aria-label` で区別する。

## B. ボタン

8. **動詞＋目的語で書く。** 「削除」より「この投稿を削除」。
   取り消しのきかない操作ほど、**確認ダイアログを読まなくても対象が分かる**ラベルにする。
   └ 出所：実例レビュー（良い例と汎用語が同一プロダクト内に混在していた）
9. **記号だけのボタンに `aria-label` を付ける。** `title` は補助であり、
   タッチでは表示されず読み上げでも確実ではない。
10. **主要な操作と副次的な操作の見た目を変える。** 同じ見た目のボタンを並べない。
11. **ボタンのラベルに絵文字を付けない。** 状態表示には使ってよい。
    └ 装飾の絵文字は、機能を表すのか状態を表すのかを毎回考えさせる。

## C. ラベルとフォーム

12. **全入力に `<label for>`。** placeholder は代用にならない（`ux.md` 3と同じ）。
13. **`title=` に説明を隠さない。** ホバーできない環境では存在しないのと同じ。
    可視テキストか `aria-describedby` にする。
14. **エラーは「何が・なぜ・どう直すか」。** 色だけで伝えない。

## D. 色（プロダクト側で規約を持つ場合）

15. **色を1つ足す前に、意味を書く。** 増えるほど「この色は何か」を毎回考えることになり、
    結局どれも意味を持たなくなる。
16. **同じ意味に違う色、違う意味に同じ色を作らない。** 定期的に実測して確認する。
    ```
    grep -rhoE '\b(text|bg|border|ring)-([a-z]+)-[0-9]{2,3}' <src> \
      | sed -E 's/^[a-z]+-//' | sed -E 's/-[0-9]+$//' | sort | uniq -c | sort -rn
    ```
    └ 出所：実在プロダクトの `docs/ui-colors.md`。7色が混在し、
      同じ意味に違う色が当たっていた実例から。**規約を作るだけでは実装に届かない**ので、
      上記の集計をCIに入れて規約外の色を落とす。

## この層を属人化させない

`forbid.md` / `ux.md` は測定器があるので自動で守れるが、**この層は放っておくと
「気づいた人が直す」に戻る**。次を必ずセットにする。

1. **プロダクト側に規約ファイルを置く**（`docs/ui-colors.md` のような、意味を定義したもの）
2. **規約のうち機械判定できるものをCIに入れる**（色の集計、絵文字、`aria-label` の有無、
   `aria-current` の有無、記号だけのボタン）
3. **判定できないものはPRテンプレートのチェック項目にする**
4. **規約を作ったら実測して差分をイシューにする。** 作っただけでは実装に届かない
   （実例では制定当日に規約外の色が16箇所残っていた）

## 出典

- Nielsen Norman Group https://www.nngroup.com/articles/
- Baymard Institute https://baymard.com/ （42,000時間のEC実測）
- GOV.UK Design System https://design-system.service.gov.uk/
- WCAG 2.2 https://www.w3.org/TR/WCAG22/
